[package]
name = "relax-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
relax-core = { path = "../relax-core" }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
toml = "0.8.20"
